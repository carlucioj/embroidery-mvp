# Embroidery MVP — Python Backend Dependencies
# Install with: pip install -r requirements.txt

# Web server
fastapi>=0.136.0
uvicorn>=0.46.0
python-multipart>=0.0.27

# Image processing
Pillow>=12.0.0
numpy>=2.0.0
opencv-python>=4.10.0

# Background removal (AI model — downloads ~170MB on first run)
rembg[cpu]>=2.0.75

# Embroidery file generation
pyembroidery>=1.4.0
