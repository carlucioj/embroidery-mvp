# Infrastructure Tests

Integration tests for the infrastructure layer will be added in subsequent tasks:

- `python/python_bridge_test.dart` — MethodChannel communication tests (Task 4.2)
- `http/processing_api_client_test.dart` — HTTP client tests (Task 4.4)
- `export/export_manager_test.dart` — Export manager tests (Task 8.5)
